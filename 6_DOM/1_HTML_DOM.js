
// HTMl DOM

/**
    DOM - Document Object Model
     // Gồm 3 thành phần:
        + Element: các phần tử, thẻ tag trong trang web (<html>, <meta>, <h1> ...)
        + Atribute: các thuộc tính của thẻ
        + Text: chữ hiện ra trên màn hình

        --> gọi chung là [node]

     // Note: 
        + HTML DOM không phải của Javascript
        + Javascript chỉ cung cấp công cụ để truy xuất vào HTML DOM
        + Browser: HTML -> DOM -> WEB API
    
     // Document Object
        + Là element lớn nhất đại diện cho cả website chứa tất cả các element khác
        + Vì vậy muốn lấy các elements khác đều phải đi qua "document"
 */